# ものさし（長さ読みアプリ）設計書

作成日: 2026-06-10
シリーズ: たねまき授業デモツール
状態: 設計確定（実装前）

---

## 1. 概要

ものさしが指し示す位置の長さを「何cm／何cm何mm」で読み取る、子ども向けの長さ学習アプリ。
ものさしの上に「測るもの」（リボン等）が乗っていて、その端を三角マーカーが指す。0からマーカーまでの長さを、テンキーで入力して答える。

- 1アプリ＝1スキル（「読む」ことに専念。長さの計算は入れない。第2弾で別アプリ化）
- 最初に「どれを えらぶ？」のホーム画面で3ステージから選ぶ（メイン＝ものさしをよむ／ミリでこたえる／とちゅうからよむ。詳細は §4）
- 既存たねまきシリーズと同じく `index.html` 1枚の軽い作り。GitHub Pages で公開
- 個人情報・会員情報は一切扱わない（Pages 公開で問題なし）

---

## 2. 画面構成（横向き専用）

ものさしが横長のため、スマホ・タブレットを **横向き** で使う前提で最適化する。
縦持ちで開いたら「よこむきにしてね」の案内を出す。

画面遷移:

- **ホーム画面**「どれを えらぶ？」… 3ステージへ分岐（上段に「ものさしを よむ」を大きく、下段に2つ）。詳細は §4
- **ステージ画面**（下記レイアウト）… 選んだステージのプレイ画面。「もどる」でホームへ

ステージ画面レイアウト（上から）:

1. 操作バー … （ものさしをよむのみ）レベル★1〜4／ものさし切替／モード切替／もどる。すべて「選択中を塗る」セグメント方式
2. ものさし＋測るもの … 画面いっぱいの横長エリア。ものさしの上にリボン等、その端に三角マーカー
3. 下段 … 左に答え枠（□cm □mm、または「ミリでこたえる」は □□mm）＋判定メッセージ＋カウンタ、右にテンキー

実機スマホ幅が厳しいので、左右はできるだけ詰める。ものさしは左寄せ。

---

## 3. 2種類のものさし（いつでも切替）

操作バーのセグメントで、どのレベル・どの問題でもいつでも切り替えられる。

### やさしい定規
- コードで描画（SVG）
- 数字つき・目盛りが大きく読みやすい・丁寧
- 左端は0ぴったりにせず、0の手前に少し余白を持たせる（本物らしさ＋端に触れて見づらいのを防ぐ）

### 木のものさし（竹尺）
- ぽこぴぃ提供の竹尺画像をそのまま使用（`assets/takejaku-original.png`）
- 縦長画像を90°回転して横向きにし、左の0側から約11cmを切り出して使う
  - 横向き版: `assets/takejaku-horizontal.png`（2146×733px、右端に「30cm」）
  - 切り出し版: `assets/takejaku-crop-11cm.png`（790×733px、左端が0）
  - ※「30cm」の文字は10cm表示では誤りになるので切り出しで除外済み
- 数字なし・cmマス目・黒い点・赤い半円。わざと読みにくい本物の質感
- 実装時、印刷された目盛りと、画面上の0・各cm位置を **ピクセル単位で較正** する（測るもの・マーカーが目盛りと正確に重なるように）

両ものさしとも「測るものとマーカーを上に乗せる背景」という同じ役割。読み取りの仕組み（測るものの位置・マーカーの位置・判定）はどちらでも共通で動く。

---

## 4. 3つのステージ（最初に「どれを えらぶ？」で分岐）

起動すると、まず最初のホーム画面で3つから選ぶ。「読む力の練習」と「換算・差読み」は別の力なので、入口から分ける（読む階段に混ぜない）。換算・差読みは“いつでも行ける場所”ではなく、この最初の分岐からしか入れない。

| ステージ | 内容 | ★階段 |
|---|---|---|
| **ものさしを よむ**（メイン） | 0からの長さを読む練習 | ★1〜4 |
| **ミリで こたえる** | 長さを「何十何mm」で答える（単位換算の芽） | なし（単発） |
| **とちゅうから よむ** | 0スタートでない長さを読む（始点と終点の差＝実質ひき算） | なし（単発） |

- ホーム画面のレイアウト: 上段に「ものさしを よむ」を大きく厚く（メインと一目でわかる）、下段に「ミリで こたえる」「とちゅうから よむ」を控えめに並べる。タイトルは「どれを えらぶ？」。情報量は最小（副題なし）
- 各ステージの中では、ものさし切替・れんしゅう/チャレンジ・「もどる」（ホームへ）が使える
- ★1〜4 が並ぶのは「ものさしを よむ」の中だけ。換算・差読みには★を付けずシンプルに保つ

### ものさしを よむ（★1〜4）

出題はすべて **最大9cm9mm（99mm）まで**。10cmぴったりは出さない（答えのcmを常に1桁に保つため。詳細は §6）。
設計原則は **1段に新要素1つ**（どこでつまずいたか診断できる階段にする）。

| レベル | 内容 | 出題範囲 | 答え方 |
|---|---|---|---|
| ★1 | 何cmぴったり | 1〜9cm（10mm刻み） | □cm のみ |
| ★2 | ◯cm5mm（“半分”の感覚） | 1〜9cm（5mm刻み） | □cm □mm |
| ★3 | 同じcm台で1mm刻みを数える | あるcm台の +1〜9mm（0cm台＝ミリだけも少し含む） | □cm □mm |
| ★4 | cmをまたいで1mm刻み・境界厚め | 11〜99mm。「◯cm9mm／次のcm直前」を多めに出す | □cm □mm |

- ★2の5mmは“半分”で読めてしまうため、★3で「同じcm台の中を1mm刻みで数える」集中練習を挟んでから、★4でcmをまたぐ
- ★4は境界（◯cm9mm、次のcm直前）のつまずきを厚めに出す

### ミリで こたえる（換算の芽）

- 長さを「何十何mm」だけで答える（例：7cm4mm の位置 → `74` mm）。出題は 11〜99mm
- 答え枠は「□□mm」1つ。2桁入力（§6）
- これは「読む」ではなく cm/mm↔mm の単位換算スキル。だから★階段に混ぜず別ステージにした

### とちゅうから よむ（差読み）

- リボン等が0でない位置から始まる。0〜マーカーではなく、始点マーカー〜終点マーカーの長さを読む
- 始点側に灰色マーカー、終点側に赤マーカーを添える
- 答え方は □cm □mm。読むだけの顔をして実質ひき算

> 設計メモ（2026-06-10 外部レビュー反映）: 旧★5階層（★4=換算／★5=非0始点）は「読む練習」に別スキルを混ぜて診断不能になるとの指摘を受け、★階段を読む力だけの4段に純化し、換算・非0始点を別ステージへ移した。★5（非0始点）は本人承認済み機能のため削除せず別ステージとして存続。

---

## 5. モード（2種類）

### れんしゅう
- まちがえたら「ざんねん！ もういっかい」で **同じ問題をやり直し**（正解は教えない。自分で数え直す）
- 正解で次の問題へ
- 「れんぞく せいかい ○もん」を表示

### チャレンジ
- 10問連続・時間制限なし・1問1回勝負
- 終わったら「10もんちゅう ○もん せいかい！」の結果画面
- 全問正解でちょっとした演出

---

## 6. 入力・操作仕様（触りやすさ最重視）

子どもがパッパッと入力できるテンポを最優先。

- **各枠は基本1桁**。数字を1つ入れたら **自動で次の枠（mm）へカーソル移動**
- 2桁入力があるのは **「ミリで こたえる」ステージの □□mm 入力のときだけ**（mm枠で最大2桁、自動移動なし）
- **確定は必ずOKボタン**（「OK→」）。自動確定はしない（見直し・訂正の余地を残す）
- 空の枠でバックスペース → 前の枠（cm）に戻る（押し間違いをすぐ直せる）
- 答え枠はタップで cm／mm を選択可。今アクティブな枠を色でハイライト
- 操作バーは「選択中を塗る」セグメント方式で、今どの状態かが一目でわかる

---

## 7. 判定仕様

長さ = 終点 − 始点（mm）。

- ★1（□cm のみ）: cm入力 == 長さ÷10
- 「何十何mm」（□□mm）: mm入力 == 長さ
- □cm □mm: cm入力 == 長さ÷10の整数部 かつ mm入力 == 長さの1の位

---

## 8. 測るもの

リボン・えんぴつ・ひも・テープなど数種類をランダムに表示（飽き対策）。
見た目が変わるだけで、読み取りの仕組みは共通。

---

## 9. コード構造（将来の再利用を見据える）

- ものさしを描く／較正する部分を **独立した部品** として書く
- 第2弾「長さの計算アプリ」（3cm2mm＋4cm5mm のような単位ごとの足し算・繰り上がり）で、この描画部品をそのまま再利用できるようにする
- 計算アプリは盤面の形が変わるため別アプリに分ける（このアプリには計算を入れない）

---

## 10. やらないこと（YAGNI）

- 長さの足し算・引き算などの計算（→ 第2弾の別アプリ）
- 10cmぴったり／10cm超の出題
- 時間制限つきモード（じっくり数える練習に合わない）
- ログイン・スコア保存・個人情報の扱い

---

## 11. 公開・登録

- GitHub Pages で公開（既存シリーズと同じ）
- 公開後、`~/Workspace/apps/REGISTRY.md` に追記する
